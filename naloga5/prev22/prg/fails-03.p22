
typ Person = {
  name : ^char,
  lastname : ^char,
  age : int
}

fun constructMe(ime:^char, priimek:^char, starost:int) : person = {
  me.name = ime;
  me.lastname = priimek;
  me.age = starost;
  me;
} where {
  var me : Person
}
